# Copy to .env.local and fill in. .env.local is gitignored.

# Used by /api/vex, the server-side proxy for the Vex chat in CYBER_BREACH.
# Get one from Google AI Studio. Do NOT paste it into any file under public/ —
# that is exactly the bug this route exists to fix.
GOOGLE_AI_API_KEY=
